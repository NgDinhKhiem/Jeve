// runtime_object_creation_insert_delete.jeve
print("Before loop:");
debug_gc();

arr = [];
for i = 0 to 100 {
    insert(arr, 0, i); // Insert at the beginning
}

debug_gc();

clean_gc();

for j = 0 to 100 {
    delete(arr, 0); // Remove from the beginning
}

clean_gc();

debug_gc();

arr = [];
for i = 0 to 500 {
    insert(arr, 0, i); // Insert at the beginning
}

debug_gc();

clean_gc();